# Limpa a area Environment
rm(list = ls())
# Verifica se existe o package xlsx, caso não ele é baixado e instalado.
if(!"xlsx" %in% installed.packages()) install.packages("xlsx")
library("xlsx")
data = read.xlsx("./dados/exercicio1.xls", encoding = "UTF-8", sheetName = "Plan1")
data = read.xlsx("./dados/exercicio1.xls", encoding = "UTF-8", sheetName = "Plan1")
head(data)
